export const menuBtn = document.querySelector('.menu-btn');
export const savePdfBtn = document.querySelector('.save-pdf');

export const pageSelectors = {
  page: '.page',
  pagePdfMode: 'page_type_pdf',
  menuBtn: '.menu-btn',
  menuBtnPdfMode: 'menu-btn_type_pdf',
  savePdfBtn: '.save-pdf',
  link: '.section__link'
}
